{{ form_start(form, {'action': path('app_resultat_editb', {'id': resultat.id})}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
        
{{ form_end(form) }}


